// Ambient module placeholder for the TypeScript project scaffold.
//
// This file exists so `tsc --noEmit` has at least one compilable unit
// while the scaffold has no feature code yet (see openspec issue #1,
// "Repo scaffold & CI foundation"). It is safe to remove once real
// source files land under `src/`.
export {};
