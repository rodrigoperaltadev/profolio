name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test (coverage)
        run: npm run test

      - name: Build
        run: npm run build

      - name: Theme build proof (real astro build, emitted CSS/HTML inspection)
        run: npm run verify:theme

      - name: Admin access gate proof (real server + real HTTP requests)
        run: npm run verify:admin-server

      - name: Profile export/import round-trip proof (real server + real HTTP requests)
        run: npm run verify:profile-export-import

      - name: Content collections proof (real getCollection())
        run: npm run verify:content

      - name: Frontmatter round-trip proof (real astro build)
        run: npm run verify:frontmatter

      - name: Env file loading proof (real --env-file spawn, no env-option injection)
        run: npm run verify:env-file

      - name: Public content routes proof (real astro build, home/listing/detail HTML inspection)
        run: npm run verify:public-routes

      - name: Admin listing unfiltered proof (deleted/draft entries still visible in /admin)
        run: npm run verify:admin-listing
